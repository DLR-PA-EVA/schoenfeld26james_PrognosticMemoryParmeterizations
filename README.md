# Code repository

Accompanies the paper *[title / citation — fill in]*.

This repository contains two experiments:

- [`L96/`](L96) — Lorenz-96 (L96) experiments.
- [`DYAMOND/`](DYAMOND) — DYAMOND experiments.

Large data artifacts (trained networks, model outputs, `.nc`/`.pkl` data files, logs)
are intentionally excluded via `.gitignore` and kept outside of version control.
